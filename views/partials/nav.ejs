<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DayTripper</title>
    <link rel="stylesheet" href="/stylesheets/main.css">
    <link rel="stylesheet" href="/stylesheets/form.css">
</head>

<body>
    <nav>
        <div id="left-nav">
            <a href="/" class="home-link">DayTripper</a>
        </div>
        <div id="right-nav">
            <a href="/itineraries/europe">Europe</a>
            <a href="/itineraries/americas">Americas</a>
            <a href="/itineraries/asia">Asia</a>
            <a href="/itineraries/africa">Africa</a>
            <% if (message && message.type==='success' ) { %>
                <div class="flash flash-success">
                    <%= message.text %>
                </div>
                <% } %>
                    <% if (user) { %>
                        <a href="/profile">
                            <%= user.username %>
                        </a>
                        <a href="/auth/sign-out" class="nav-button">Sign out</a>
                        <% } else { %>
                            <a href="/auth/sign-in" class="nav-button">Sign in</a>
                            <% } %>
        </div>
    </nav>